package org.example;

public class Main {
    static void main() {
        IO.println(LeapYears.isLeapYear(2020));
        IO.println(LeapYears.isLeapYear(2000));
        IO.println(LeapYears.isLeapYear(2015));
    }
}
